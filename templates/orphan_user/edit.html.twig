{% extends 'base.html.twig' %}

{% block body %}
    <h1>Modification d'un utilisateur</h1>

    {{ include('orphan_user/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_orphan_show', {'id' : orphan_user.id }) }}">back to list</a>

    {{ include('orphan_user/_delete_form.html.twig') }}
{% endblock %}
